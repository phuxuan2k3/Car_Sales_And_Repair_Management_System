const FixedCar = require('./models/fixedCar');

const TestFunction = async () => {
    // const data = await FixedCar.getAll();
    // const data = await FixedCar.getFixedCarByCusId(402);
    // console.log(data);
}

TestFunction();
